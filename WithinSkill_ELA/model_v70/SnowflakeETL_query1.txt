CREATE OR REPLACE TABLE [REDACTED].PRODUCT_TESTING.ELA_QUESTIONID_SKILLID_ASSOCIATIONS AS
SELECT DISTINCT
    QUESTIONID,
    ARRAY_COMPACT(
        ARRAY_CAT(
            COALESCE(
                FIRST_VALUE(QUESTION_SKILLS) OVER (
                    PARTITION BY QUESTIONID
                    ORDER BY OCCURREDAT DESC
                ),
                ARRAY_CONSTRUCT()
            ),
            ARRAY_CONSTRUCT(
                FIRST_VALUE(RLSKILLID) OVER (
                    PARTITION BY QUESTIONID
                    ORDER BY OCCURREDAT DESC
                )
            )
        )
    ) AS UNIQUE_SKILLS
FROM PROGRESS.ELA_ANSWER_EVENTS
WHERE OCCURREDAT >= '2024-05-01'
ORDER BY QUESTIONID